[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "ai-vulnerability-researcher"
version = "0.5.0"
description = "Authorization-scoped automated source vulnerability research toolkit"
requires-python = ">=3.9"
dependencies = []

[project.scripts]
vulnresearch = "vulnresearch.cli:main"

[tool.pytest.ini_options]
pythonpath = ["."]
